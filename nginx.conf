server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;

    # Redirect root to farmazed-web landing page
    location = / {
        return 301 /farmazed-web/;
    }

    location / {
        try_files $uri $uri/ =404;
    }
}
